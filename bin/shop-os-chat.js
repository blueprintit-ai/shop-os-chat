#!/usr/bin/env node
// Entry point. Filled in by Task 10. For now, just prints and exits so
// `npx shop-os-chat` resolves.
console.log("Shop OS Chat — entry point not yet implemented.");
process.exit(0);
